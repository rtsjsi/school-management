# Supabase (get from Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Required for correct role-based access (server-only, never expose to client)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: School name and address for receipts and report cards
NEXT_PUBLIC_SCHOOL_NAME=School Name
NEXT_PUBLIC_SCHOOL_ADDRESS=Address, City
