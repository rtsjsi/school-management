<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <runtime>
    <!-- Allow catching AccessViolation from SBPCCOMM.dll instead of killing the process. -->
    <legacyCorruptedStateExceptionsPolicy enabled="true" />
  </runtime>
</configuration>
